# TouchDesigner workspace

Grandmaster Melodia uses TouchDesigner as the live polish layer: audio analysis, OSC beat routing, Envoy MCP on `localhost:9870`, and procedural preview before captures freeze into Unreal.

This Pages checkout only ships this note. The `.toe` networks, Escher SOP gallery, and render dumps live in the local `_TouchDesigner/grandmaster_melodia/` workspace, not in `my-site`.

See [TouchDesigner architecture](../wix/touchdesigner-architecture.html) for ports, scripts, and how TD sits between Blender exports and UE.
