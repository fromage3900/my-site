# WebGL Submission / Git State Audit — 2026-09-10

**Refreshed:** final repo-side consolidation  
**Purpose:** distinguish canonical source, preserved capture evidence, and the machine/human checks that remain before submission.

## Executive state

Nothing important in today's three application lanes is stranded in an agent workspace as an undocumented repo dependency.

`fromage3900/my-site` is the authority for the website, WebGL kit, application drafts, submission manifest, tracker snapshot, runtime/provenance checklists and continuation handoffs. `fromage3900/MelodiaMelusinaV2` remains the game/capstone authority; its industrial transfer PRs are closed archives and must not be re-applied over current website source.

The WebGL kit remains inside the explicit recruiter-site freeze exception under `tools/fromage-webgl-kit/`; this does not authorize unrelated public-site redesign.

## Lane 1 — $10k procedural industrial homepage loop

**State:** FINAL SOURCE CANONICAL / ONE NORMAL-BROWSER SANITY CHECK BEFORE SEND.

Canonical on `my-site/main`:
- final industrial `app.js` source;
- polished/capture-ready `index.html`;
- shared deterministic timeline helper;
- final proposal `prototypes/procedural-industrial-loop/APPLICATION_DRAFT_AFTER_QA.md`;
- refreshed `EVIDENCE.md` and `LISTING_10000_EVIDENCE.md`;
- `INDUSTRIAL_CAPTURE_ARCHIVE_RECEIPT.json`;
- submission manifest, canonical handoff and application-tracker CSV.

Current source implements the documented buyer-facing behavior: fixed camera, RoomEnvironment reflection response, ACES tone mapping, restrained studio shadows, shared BODY/SHELL/ACCENT/MACHINE/ACTUATOR materials, rounded fabricated housings, sparse manufacturing detail, greyscale BASE, information-only Structural/Flow/Thermal modes, deterministic capture URLs, Seed 01 canonical, diagnostics disclosure, and the unchanged 16-second absolute-time motion law.

Cursor's preserved capture receipt records zero console errors, `LOOP Δ = 0`, 81 draws, 16,948 triangles, a full 16-second BASE capture, hero/visualization stills, seed captures and mobile evidence. Those measurements belong to Cursor's captured build; headless FPS values are not buyer-facing performance promises.

Storage policy: generated PNG/MP4/WebM captures remain in closed Melodia PR #176 as immutable capture history instead of being duplicated into the source repo. `my-site/main` contains the exact archive pointer, file inventory and observed-value receipt. PR #175 is closed and superseded; PR #176 is closed without merge. Do not apply either patch over current website source.

**Remaining outside Git:** open current main in a normal browser, verify one deterministic capture URL and the loop boundary, inspect the five final attachments, recheck the listing, then submit after human review.

## Lane 2 — Three.js fabric technical-art consultant

**State:** CANONICAL DOCS + EXECUTION CHECKLISTS / RUNTIME + PROVENANCE + OWNER PROOF NEEDED.

Canonical on `my-site/main`:
- `fabric/FABRIC_CONSULTANT_EVIDENCE.md`;
- `fabric/CLO_TO_WEB_PIPELINE.md`;
- `fabric/APPLICATION_DRAFT_AFTER_QA.md`;
- `fabric/FABRIC_RUNTIME_QA.md`;
- `fabric/FABRIC_SUBMISSION_ASSET_CHECKLIST.md`;
- `fabric/MATERIAL_PROVENANCE_MANIFEST.json`;
- existing `wix/` fabric/material viewer architecture used as source evidence.

The provenance manifest intentionally begins unclear resources as `UNKNOWN` and non-reusable. This prevents accidental commercialization of uncertain or Melodia-specific textures while still allowing the architecture to be discussed and evaluated.

Still required outside Git before submission:
- execute the viewer smoke/material-inspection QA;
- finish exact source/license classification for any material actually shown as reusable;
- select one genuine Brennan-owned CLO or Blender artifact;
- choose concise explainable evidence links;
- recheck the listing before send.

A full knitwear configurator is not required for the consulting listing.

## Lane 3 — $3k web-ready product / GLB animation

**State:** AURIGA SOURCE + HANDOFF CANONICAL / BLENDER GENERATION + RUNTIME/CAPTURE NEEDED.

Canonical on `my-site/main`:
- `scripts/build_auriga_meter.py`;
- `scripts/check_auriga_meter.py`;
- `prototypes/product-motion-lab/src/auriga-meter.js`;
- `prototypes/product-motion-lab/package.json`;
- Product Motion Lab `index.html`, `app.js`, `EVIDENCE.md`;
- `prototypes/product-motion-lab/PRODUCT_RUNTIME_HANDOFF.md`.

This supersedes the old note that useful product source existed only in Kimi's workspace. The repo now has the generator, checker, loader and exact continuation order. The generated `assets/auriga_meter.glb` is deliberately not called evidence until Blender actually produces it and the exported file passes validation/runtime review.

Still required outside Git before submission:
- run the canonical Blender builder;
- perform Brennan visual/silhouette review;
- run the GLB checker;
- browser-load the GLB through the canonical loader;
- verify deterministic scrub and all three anchors;
- capture desktop/mobile proof and measured diagnostics;
- recheck the listing before send.

## Repository closure rules

- `my-site/main` is the sole website/WebGL-kit source of truth.
- Melodia PR #175 is superseded and closed.
- Melodia PR #176 is archive-only and closed without merge.
- No unpushed Kimi/Cursor workspace is required to continue any lane.
- No new commercial prototype is needed today.
- No public recruiter-site redesign is part of these application lanes.
- Do not promote agent-reported, source-inspected or headless-only measurements to buyer-facing claims beyond the exact archived evidence scope.
- Runtime/owner gates remain intentionally open until observed; they are not Git loose ends.

## Canonical decision summary

```text
INDUSTRIAL: SOURCE + DOCS + CAPTURE RECEIPT CANONICAL / EXTERNAL SANITY + HUMAN SEND ONLY
FABRIC:     DOCS + QA + PROVENANCE GATES CANONICAL / VIEWER + OWNER PROOF + HUMAN SEND
PRODUCT:    AURIGA SOURCE + HANDOFF CANONICAL / BLENDER + GLB + BROWSER PROOF + HUMAN SEND
CAPSTONE:   CHORAL SHEEP REMAINS P0 AFTER THE APPLICATIONS
```

There are no additional repo-architecture, transfer, branch, or documentation tasks required for these three application lanes. Remaining work is deliberately execution/evidence/human-send work and must not be disguised as completed Git work.